# Motion Wow · W28D01

```yaml
status: draft_self_generated
content_id: W28D01
review_source: draft_self_generated
prototype_allowed: true
final_render_allowed: false
```

> 目标：验证“重复动作计数器”是否真的进入像素，而不是退回大字卡片/四卡片清单。  
> 当前允许：低保真 HTML/动效样机。  
> 当前禁止：最终 TTS / 最终 render / approved。

## CREATIVE 核心

本条唯一主创意：**员工一天重复动作计数器。**

不是把“高频、重复、输入、结果”做成四张卡片，而是让观众看到：

1. 高级 AI 方案被现实工作打断。
2. 每一次复制、判断、改写都让计数器累加。
3. 计数器累计到 10 后冻结，反转成“流程没产品化”。
4. 四问题从动作轨迹中抽出。
5. CTA 是动作输入条，而不是固定评论框。

## 逐镜动效承诺

| 镜头 | 必须出现的动效 | 不合格表现 | 验收方式 |
|------|----------------|------------|----------|
| s1_hook_interrupt | 《高级 AI Agent 方案》被客户消息弹窗打断；复制动作触发计数器 0→1 | 只有大字钩子，没有打断和计数 | 0-3s 截帧可见方案、弹窗、计数器 |
| s2_real_questions | generated_fact 示意需求便签压住方案文件，第四条变红，明确不冒充真实原话 | 四条问题像真实用户原话 | 截帧可见便签压住方案和示意标注 |
| s3_loop_morning | 复制消息进表格，每次复制计数 +1 | 数字自己跳，与动作无关 | 计数变化与光标/复制动作同步 |
| s4_loop_noon | 备注高亮，计数器从“重复动作”切到“重复判断” | 仍是复制动作重复播放 | 截帧可见备注列高亮 |
| s5_loop_afternoon | 三条相似回复重叠，变量词替换，计数器累加 | 普通聊天截图 | 变量词闪动明显 |
| s6_freeze_reversal | 重复动作轨迹叠加，计数器停在 10，画面冻结后出反转句 | 黑底大字页 | 截帧必须同时有轨迹、计数器、反转句 |
| s7_filter_from_loop | s6 轨迹延续并拆成四个判断点 | 四张卡片清单 | 动作轨迹到四问题有可见转换 |
| s8_action_cta | 计数器归零，动作输入条出现，示例动词浮现 | 固定评论框 | CTA 主焦点是动作空槽 |

## 音效与节奏

- s1: 0.8s 使用 `paper_snap` 打断方案文件。
- s3-s5: 每次计数 +1 使用轻 `tick`，不要吵。
- s6: 冻结时使用低频 `freeze_hit`。
- s8: 输入条出现使用轻 `input_pop`。

## 像素验收清单

- [x] 0-3s 不是纯大字。低保真截图见 `prototype/qa_shots/s1.png`。
- [x] 首镜计数器进入安全区。已修复右侧裁切。
- [ ] 计数器至少出现 5 次有效变化。HTML 已有阶段切换，正式动效仍需逐帧验收。
- [ ] 中段不是卡片清单。低保真结构已避免普通卡片，正式动效仍需复核。
- [ ] s6 不是纯黑底金句页。HTML 已包含轨迹/冻结/反转，正式节奏仍需复核。
- [ ] s7 四问题不是独立四卡片。低保真已改为轨迹拆解，生产级仍需动态复核。
- [x] CTA 不是固定评论框。低保真为动作输入条。
- [x] generated_fact / synthetic_visual 在 asset_log 标清。

## 当前复评状态

- 形式复评：方向可做，但建议先低保真样机。
- 内容复评：脚本可用，但不得把 generated_fact 冒充真实原话。
- 编导决议：允许 prototype，不允许 final render。
- 原型 QA：`prototype_visual_qa_pass_with_limits`，正式 pre_render 仍 blocked。
