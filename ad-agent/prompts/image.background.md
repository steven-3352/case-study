# 背景生成提示词（03_keyframes · display 镜）

这段基调会拼在每个展示帧的背景生成前面。展示帧的背景由 AI 生成，产品原图随后由程序
像素级 paste 上去（不重绘），所以这里**只描述背景，绝不描述产品本身**。

## 硬要求

- 只画背景/环境/光影/质感，**画面里不能出现任何产品、主体、人物、文字、logo**——
  中心留出干净区域给产品放置。
- 与产品调性一致的场景（如高端产品→质感台面/柔光；食品→温暖厨房/餐桌）。
- 禁蓝紫主色、禁 AI 味深色堆叠；构图干净、景深柔和，突出中心留白。
- 光线方向统一、可信，便于产品叠上去后不违和。
