# 首帧生成提示词（03_keyframes · generated 镜）

这段基调会用于生成镜（generated）的 AI 首帧图。生成镜负责氛围/场景/情绪，允许画面自由。

## 要求

- 按分镜的 `image_prompt` / `scene` 画完整画面（可含产品作氛围，但不要求精确——
  精确展示交给 display 展示帧）。
- 电影感构图、可信光影、真实材质；避免塑料感与过曝。
- 禁蓝紫主色、禁 AI 味深色堆叠；跟需求 tone 走。
- 若给了产品参考图，画面里的产品保持大致形态即可（i2v 会动，允许漂移）。
