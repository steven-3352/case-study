"""引擎级几何常量 —— 画幅 · 平面画布余量 · 景别表。

**per-film 的东西不放这**:调色板、素材路径、SEG 时间轴、字体路径,由每片自己的
`voice_room/<film>/` 声明。这里只放跨片可共享的默认值,以及 `景别→占比` 的翻译表
(分镜写景别、渲染要占比,这张表是两者之间**唯一**的翻译处)。

**为什么留 `PAD_W/PAD_H`**:相机做俯仰透视时要采样到取景框以外的内容,所以平面
先画在加宽画布上再变形。不留余量,仰拍/俯视时画面边缘会出现采样不到的黑边。
`dx_max = W*0.50, dy_max = H*0.30` → `PAD_W = W*2.0, PAD_H = H*1.70`。
"""
from __future__ import annotations

W, H = 1920, 1080
FPS = 30

PAD_W, PAD_H = int(W * 2.0), int(H * 1.70)
OX, OY = (PAD_W - W) // 2, (PAD_H - H) // 2

# 分镜写的是景别,渲染要的是数;这张表是两者之间唯一的翻译处。
# 「占比」= 主体 bbox 面积 / 画幅面积。大特写 >1 表示主体溢出画幅(脸切边)。
FRAMING = {
    "极端全景": 0.05,
    "全景": 0.13,
    "中景": 0.30,
    "近景": 0.48,
    "特写": 0.72,
    "大特写": 1.15,
}
