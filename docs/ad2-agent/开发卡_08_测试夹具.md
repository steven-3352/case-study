# 开发卡 08 · 测试夹具与回归（§28）

> 只读本卡。你要交付「pytest 契约测试 + 帧指纹回归」。**复用现有测试基座，不新建框架。**

## 目标

provider 契约、媒体变换、镜型分流、安全门都有自动化测试；视觉产物用确定性帧指纹做回归基线。

## 直接用这些脚本（已存在，照抄断言风格）

- 参考模板：`tests/mvstudio/providers/test_seedance.py`——fake transport 断言请求体、`metadata.content[]` 加法式结构、参考 URL 安全校验、transport error 脱敏。**新测试照它写**。
- 帧指纹：`tests/support/frame_digest.py`——生成/比对确定性帧指纹，作视觉回归。
- 端到端样例：`tests/e2e/`（六步贯通）。
- 摘要：`from mvstudio.media import sha256_bytes`（媒体变换产物应字节可复现）。

## 最小回归矩阵（每条一个测试）

| 维度 | 断言 |
|---|---|
| 镜型分流 | `display` 走 `compose_product_on_background`（产品区字节=原图）；`generated` 走 `fit_image_to_canvas` |
| Seedance 契约（无参考） | 不追加 `content[]`、`generate_audio` 保持关；`body["metadata"]` 等值断言仍成立 |
| Seedance 契约（带参考） | `content[]` 顺序 images→video→audio 与实测 curl 一致；有 audio 自动开 `generate_audio` |
| 参考 URL 安全 | 非 https（非 loopback）被拒；含凭据/query/fragment 被拒 |
| 去身份化门 | `generate_pose_reference` 覆盖率 < `min_coverage` 抛 `PoseReferenceError` |
| 本地免费路径 | `static`/`ken_burns` 镜不初始化 Seedance、无网络 |
| 局部失效 | `params["only"]` 单镜重出只改目标镜、增量并 index |
| 幂等 | 同 `idempotency_key` 重复提交不重复扣费、不产不可追溯副本 |

## 夹具版本化

- 参考素材与期望产物打 `fixture_version`（对应能力清单 `benchmark.fixture_version`）；Seedance 模型标识或关键参数变更时递增，旧基线保留不覆盖。
- 代表镜头小样的期望帧指纹随 `fixture_version` 归档，作视觉门回归锚点。

## 铁律

- provider 测试用 fake transport，**不打真接口**、不烧额度。
- 媒体测试用固定输入字节断言摘要，保证可复现。
- 「跑通一次」≠ 通过；Done 以全绿为准。

## 完成判据（Done）

- [ ] 上表每维至少一个 pytest 用例且全绿。
- [ ] 新增 provider 用例不依赖网络。
- [ ] `display` 保真、Seedance `content[]` 顺序两项有回归锁。

## 自测

```bash
PYTHONPATH=src /Users/wmzuo/Documents/project/case-study/.venv/bin/python \
  -m pytest tests/mvstudio/providers/test_seedance.py -q
PYTHONPATH=src /Users/wmzuo/Documents/project/case-study/.venv/bin/python \
  -m pytest --cov=src --cov-report=term-missing
```
